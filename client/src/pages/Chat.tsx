import ChatBox from "../components/ChatBox";

const Chat = () => {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <ChatBox />
    </div>
  );
};

export default Chat;